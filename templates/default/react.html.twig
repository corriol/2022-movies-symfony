{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('prova') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('prova') }}
{% endblock %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <div id="root">

    </div>

    <h3>Novetats</h3>
    {% if movies %}
        <div class="row row-cols-4">
            {% for movie in movies %}
                {{  include ('_movie_data.html.twig') }}
            {% endfor %}
        </div>
    {% else %}
        <p>No movies found</p>
    {% endif %}
{% endblock %}
